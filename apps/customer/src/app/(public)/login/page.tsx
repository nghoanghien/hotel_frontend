/**
 * Login Page - Routing Placeholder
 * 
 * This page exists ONLY for Next.js routing purposes.
 * The actual UI is rendered by LoginPageContent in the layout.
 * 
 * This allows us to keep both login and register in the same DOM tree
 * for perfect shared element transitions with Framer Motion layoutId.
 */
export default function LoginPage() {
  return null;
}
